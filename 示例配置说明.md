# 示例配置说明

## 文件更新

已将示例配置从 `fish_pool` 更新为 `example`，展示了所有5种字段类型的使用方法。

## 字段类型展示

### 1. 文本类型 (text)
- `config_name`: 普通文本字段，带引号导出
- `text_field`: 演示普通文本，导出时会加引号

### 2. 数字类型 (number)
- `number_field`: 数值字段，导出时不加引号

### 3. 选项类型 (option)
- `enum_field`: 下拉选项，标记为"原始文本"
  - 导出为 `ConfigType.Normal` 而不是 `"ConfigType.Normal"`
  - 用于引用代码中定义的枚举值

### 4. 数据列表类型 (datalist)
- `datalist_field`: 带标签的下拉列表，标记为"原始文本"
  - 导出为 `AreaCode.Ocean` 而不是 `"AreaCode.Ocean"`
  - 编辑时显示友好标签（如"海洋区域"），导出时使用值

### 5. 条目类型 (entry)
- `entry_list`: 包含子字段的列表
  - `item_id`: 原始文本，导出为枚举值
  - `item_name`: 普通文本，导出加引号
  - `count`: 数字
  - `rarity`: 选项（不是原始文本，导出加引号）

## 原始文本功能

"原始文本"复选框控制字段值在Lua导出时是否加引号：
- ✅ 勾选：导出时不加引号，如 `ConfigType.Normal`
- ❌ 不勾选：导出时加引号，如 `"普通文本"`

适用于需要引用代码中定义的常量、枚举值等场景。

## 智能字段名格式

字段名称会根据是否为合法Lua标识符自动选择格式：
- 合法标识符（如 `config_name`）：使用直接格式 `config_name = value`
- 非法标识符（如 `field-with-dash`）：使用括号格式 `["field-with-dash"] = value`

## Lua导出示例

导出的代码位于 `example_export.lua`，展示了：
- 类型注解 `@class`
- 字段类型定义
- 智能字段名格式
- 原始文本不加引号
- 普通文本加引号
- 数字不加引号
- 条目列表的嵌套结构
