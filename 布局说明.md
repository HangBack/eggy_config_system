# 新版布局说明

## 主要改进

### 1. Schema编辑面板 - 左右分栏布局

**左侧：字段列表**
- 显示所有字段的概览
- 显示字段名称、类型和标签
- 点击字段项切换编辑
- 激活的字段高亮显示

**右侧：字段编辑器**
- 所有字段属性在一行紧凑显示
  - 字段名称、字段标签、字段类型、默认值
- 必填选项和删除按钮在同一行
- 类型特定配置（选项、数据列表、条目子字段）显示在下方

**优势：**
- 清晰的字段概览
- 更高效的编辑体验
- 减少滚动，提高工作效率

### 2. 配表编辑面板 - 左右分栏布局

**左侧：数据行列表**
- 显示所有数据行
- 显示数据行名称（只读）
- 删除按钮直接在列表项上
- 激活的数据行高亮显示
- 点击切换编辑

**右侧：数据编辑器**
- **标题栏**：可编辑的数据行名称输入框
  - 修改后自动同步到左侧列表
  - 这个名称将作为Lua导出时的键名
- 显示当前选中数据行的所有字段
- 字段按Grid布局自动排列
- 条目类型字段占满整行

**优势：**
- 自定义数据行名称，便于识别（在右侧修改，避免误触）
- 左侧列表快速切换数据行
- 更直观的数据组织方式
- 名称直接用于Lua导出

### 3. 响应式设计

- 移动端自动切换为上下布局
- 侧边栏在小屏幕上变为顶部栏
- 确保在各种设备上都有良好体验

## 使用技巧

1. **快速编辑Schema字段**
   - 在右侧编辑器中修改字段时，左侧列表会实时更新
   - 方便查看字段配置的整体结构

2. **数据行命名**
   - 在右侧标题栏的输入框中修改
   - 实时同步到左侧列表
   - 建议使用有意义的名称，如"FisheggyIsland"、"Ocean"等
   - **Table格式导出时，这些名称将作为Lua表的键名**

3. **紧凑的字段配置**
   - 所有基本字段信息在一行显示
   - 减少鼠标移动距离
   - 提高配置效率

## 快捷操作

- **添加字段/数据行**：点击侧边栏顶部的 + 按钮
- **删除**：直接点击对应项的删除按钮
- **切换编辑**：点击左侧列表项
- **重命名数据行**：在右侧标题栏输入框中修改（避免误触）

## 启动方式

### Windows
双击 `start.bat` 或在命令行运行：
```bash
python web.py
```

### 浏览器访问
后端启动后，直接在浏览器中打开 `index.html` 文件即可使用。

---

享受更高效的配表编辑体验！🎉
